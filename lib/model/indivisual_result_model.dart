class IndivisualResult {
  String name, exam_name, roll, batch_name, startdate;
  int skiped_qns, correct_ans, wrong_ans, full_marks, marks_per_qns;

  IndivisualResult(
    this.skiped_qns,
    this.correct_ans,
    this.wrong_ans,
    this.full_marks,
    this.marks_per_qns,
    this.name,
    this.roll,
    this.exam_name,
    this.batch_name,
    this.startdate,
  );
}
