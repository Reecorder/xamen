class Exam {
  String name, startdate, enddate, start_time, end_time;
  int id, batchid, full_marks, mark_per_qns, exam_duration;
  Exam(
    this.name,
    this.startdate,
    this.enddate,
    this.start_time,
    this.end_time,
    this.id,
    this.batchid,
    this.full_marks,
    this.mark_per_qns,
    this.exam_duration,
  );
}
